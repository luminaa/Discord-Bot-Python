import os

token = os.getenv(TOKEN) #put ur bot token into secrets with label "TOKEN"
#example: friend1 = os.getenv(friend1)

'''
put the IDTOKEN in secrets for security as repls are public
you can put ids of discord users into secrets <@id_number> 
'''
  